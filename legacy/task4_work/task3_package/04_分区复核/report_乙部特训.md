# 乙部特训6题逐题复核报告

## 范围与方法

- 范围：`M2QD-DA-PARTB-Q1` 至 `M2QD-DA-PARTB-Q6`，共6道完整题。
- 回查：题目MMD第1370–1569行；乙部答案MMD第1–430行；Q4题目图片原图。
- 数学复核：逐题独立代数推导；另用 NumPy 有限差分与数值代入复核 Q1、Q3 的导数以及 Q4–Q6 的变化率结果。
- 分类：只使用完整题入库标准中的5个主类型、29个受控标签和 Joy Level 1–5。

## 结果

- 题目边界：6/6 完整。
- 题答配对：5/6 直接对应；Q3 来源答案草图的函数标签错误，已订正并待 Joy 确认。
- 数学结论：6/6 的复核答案正确。
- 必要题图：Q4 的1幅题图存在、可读且与题干一致；其他5题不依赖题图。
- 状态：`audit_passed=5`、`audit_pending=1`（Q3）、`blocked=0`。
- 难度：L4三题（Q1–Q3），L5三题（Q4–Q6）。

## 主要校订

1. Q1冻结答案文本出现重复 Tips、句段错位及不可读二阶导数文本；已生成干净复核答案。复核答案同时明确：$f''$ 在垂直渐近线两侧也异号，但该断点不是拐点；定义域内唯一拐点仍为 $(2^{2/3},2^{5/3}/3)$。
2. Q2答案重复 `r=3`、`p=-2`，并只用图片给出草图；已用完整关键特征补全。
3. Q3答案混入评分符号；更重要的是，草图把曲线标为 $y=x-1/(x+1)$，与题目 $f(x)=x-x/(x+1)$ 不一致。草图形状、渐近线和极值点对应正确函数；本次已记录正确标签 $y=x-1+1/(x+1)$，并将该题保留为 `audit_pending`。
4. Q5冻结答案MMD第325行写成 `A=√(k²-1)`；正确式为 `A=k√(1-k)`。现数据库答案已使用正确式，但此前没有保存校订记录，本次已补记原文、理由和证据；现有材料不足以继续断言错误发生在原版还是转换阶段。
5. 六题中文均做了不改变数学信息的简体化、术语和标点规范。

## 自检

```text
jq 'length' review_乙部特训.json -> 6
jq 'map(.question_id)|unique|length' review_乙部特训.json -> 6
NumPy verification -> NUMERIC_VERIFICATION_PASS
```
