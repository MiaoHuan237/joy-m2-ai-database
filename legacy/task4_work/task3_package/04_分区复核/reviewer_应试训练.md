# Task 3“应试训练”14题独立复核

复核日期：2026-08-03（Asia/Singapore）  
复核范围：`review_contract.md`、`review_应试训练.json`、`report_应试训练.md`、`complete_questions_45.json`、`differentiation_application_taxonomy.json`、`difficulty_levels.json`、两份冻结 MMD，以及冻结 V1.16 发布包内同哈希的正式 SQLite。所有原审计文件、冻结源和数据库均按只读方式检查。

> **最终快照：** 审计方已根据首次独立复核发现完成修订，本报告随后对最终文件重新验证。最终 `review_contract.md` SHA-256 为 `254841cedfa0e9dd36ad30bfc3fb334c13624abc91c1f4b54ab535c8d410c938`，`review_应试训练.json` 为 `b402d6b8a31fe2abae6df6d5572de93b92b8fb0066880a15de715c706028bda6`，`report_应试训练.md` 为 `6d1e407a7fb24483a142e056bd9723aeed3182b895fb96a3714eed3990c3f0cc`。首次复核的 I-1/M-1 已关闭，不计入最终问题计数。

## 总体 verdict

| 维度 | Verdict | 结论 |
|---|---|---|
| 规格符合性 | **PASS** | 14题齐全且编号连续；必填字段、枚举、受控主类型/标签、Level 与六维取值均合法；14条来源成员哈希校订满足新增契约；`audit_pending` 判定符合边界。 |
| 审计质量 | **PASS** | 题干、数学答案、Q3/Q7/Q13 分值、Q7 拐点、Q8/Q11/Q12 校订、答案来源文件/成员/成员哈希纠正均有充分证据；Q3/Q13/Q7 的答案验证状态现已一致。 |

最终发现计数：**Critical 0 / Important 0 / Minor 0**。

## 独立核对结果

### 覆盖、编号与正式库对齐

- `review_应试训练.json` 恰有14个对象，`question_id` 唯一并完整覆盖 `M2QD-DA-TRAIN-Q1` 至 `M2QD-DA-TRAIN-Q14`，`source_section` 全为“应试训练”。
- `complete_questions_45.json` 中“应试训练”同样为14题；正式 V1.16 SQLite 中相同14个整题记录的 `question_number` 连续为1–14，`is_complete_question=1`。
- 冻结题目 MMD 的训练题编号亦连续为1–14；未发现漏题、重号、拆错小问或题界串题。

### 逐题数学、分值与来源核对

下表的“题目行”均指冻结题目成员 `ab3a3800-3b11-4cb8-af40-46ce05caf9f6.mmd`；“答案行”均指冻结答案成员 `c2e97ff4-9258-43ff-802f-ac95d2c5c0e2.mmd`。

| 题 | 题目行 | 答案行 | 总分 | 独立数学结论 |
|---|---:|---:|---:|---|
| Q1 | 410–413 | 5–30 | 6 | 两交点 `(0,±3)` 及两条切线均正确。 |
| Q2 | 433–437 | 31–96 | 6 | 切点 `(1,2)`、`(-1,0)` 及两条切线正确；竖直切线特殊情形未漏解。 |
| Q3 | 494 | 98–126 | **4（恢复）** | 唯一极小点 `(0,-1/3)` 正确。 |
| Q4 | 511–522 | 127–176 | 6 | 最大值 `1/e` 及 `e^x≥x^e` 证明正确，等号在 `x=e`。 |
| Q5 | 580–583 | 177–199 | 4 | 全局最大值 `189` 正确。 |
| Q6 | 603–607 | 200–227 | 4 | 定义域端点比较后全局最小值 `-45` 正确。 |
| Q7 | 675–677 | 228–258 | **6（恢复）** | 二阶导数两次变号；全部拐点确为 `(-1,0)`、`(1,0)`。来源答案只算出函数值，未写最终坐标结论，审计补全正确。 |
| Q8 | 689–697 | 259–298 | 6 | 两个二阶导数零点均为拐点；数学结论正确。答案行285–286的相邻两个 `1M` 会令可见分值变成7，删除一个的校订正确。 |
| Q9 | 759–761 | 300–311 | 3 | 渐近线 `x=-4`、`y=4x-21` 正确。 |
| Q10 | 781–794 | 313–334 | 6 | `x=1`、`y=x-1` 为渐近线，`r''(x)>0` 且无拐点，均正确。 |
| Q11 | 847–879 | 336–365 | 8 | 极大点、极小点、无拐点、两条渐近线及曲线分支均正确；出版社图像与文字转录一致。 |
| Q12 | 881–913 | 367–383 | 8 | 极大点 `(0,-2)`、无拐点、三条渐近线及偶函数三分支均正确；出版社图像与文字转录一致。 |
| Q13 | 971–974 | 385–405 | **3（恢复）** | `dh/dt=-1/4 cm·s⁻¹`、负号及单位正确。 |
| Q14 | 976–990 | 407–448 | 5 | `A=2u ln u`，在 `u=e` 时 `dA/dt=-4e` 平方单位/秒，正确。 |

复核结论是：`solution_verified` 的数学内容14/14正确；其中13题的来源数学答案可直接通过，Q7 的来源推导正确但最终点坐标表述不完整。Q3/Q7/Q13 的4/6/3分均由答案档评分标记合计及末行总分双重支持。

### 答案来源映射

- 纠正结论成立。14题在 `complete_questions_45.json` 中均把 `solution_source_file/member` 指向题目档；冻结题目档的相应训练题片段只有题面及作答空位，而14份答案连续位于“微分的应用甲部”答案成员第5–448行。
- 正式 V1.16 SQLite 进一步交叉支持该结论：14题的 `source_ms_page` 依次指向 `part_a MMD lines 5–30` 至 `407–448`，且 `sources.solution_file` 列出“微分的应用甲部.mmd.zip”。
- 冻结哈希一致：题目压缩包为 `c98efaa…f0749`，答案压缩包为 `7306fc…471b5`；答案成员 SHA-256 为 `bfd75702b5abff3f604bfeb722f6ab7cd6c23566d15ea550bbf05be1b4dbbeb0`。

因此，14条 `database_mapping` 校订的目标文件、成员和行号证据充分。

### 标签、主类型与 Level

- 全部主类型和标签均存在于受控 taxonomy；未发现非法值、空标签或仅因解答顺带使用方法而滥贴标签。
- 主类型分布复算为：切线与法线2、极值与曲线性质8、最值与最优化2、变率2，与报告一致。
- Level 分布复算为：L2三题、L3七题、L4四题；六维总分分别落在难度标准的通常参考带内：L2为4/4/5，L3为6–7，L4为8–10。
- 相比正式库旧 Level，Q3/Q13 降为L2，Q4/Q6/Q8/Q14降为L3，Q11/Q12降为L4；依据新标准均可解释：前两题为直接常规步骤，四道L3为标准多步题，Q11/Q12虽为全局曲线分析但已给导数符号表，稳定要求未达到L5。

### 校订记录与审计状态

- Q3/Q7/Q13 分值恢复、Q7 最终拐点结论、Q8 重复评分标记、Q11/Q12 中文正负号 OCR 修复，均能在冻结行号或原图哈希处复现。
- 14题保留 `audit_pending` 合理：契约明确要求来源文件/成员映射在正式记录修复前不得通过；Q3/Q7/Q13 另有待落库分值，Q7 另有待落库的完整结论。
- 没有题界、核心条件、关键输入图像或答案缺失到无法可靠使用的题，故 `blocked=0` 合理。

## Findings

### Critical（0）

无。未发现漏题、错题配答、数学结论错误、错误分值或应阻断却未阻断的记录。

### Important（0）

无未关闭 Important。

首次复核发现的 I-1（来源映射修复未同步覆盖 `solution_source_member_sha256`）已关闭：

- 最终契约已把 `source_member_sha256` 与 `solution_source_member_sha256` 纳入允许校订字段，并明确成员映射变化时必须同步记录旧、新哈希。
- 最终 JSON 的14题各恰有一条 `field="solution_source_member_sha256"`、`error_origin="database_mapping"` 的校订，统一从题目成员哈希 `4b963982…1fac5` 更正为实算答案成员哈希 `bfd75702…beb0`。
- 14条均含题号对应的答案行号证据；报告也同步写出旧值、新值、计算方式与自检命令。

### Minor（0）

无未关闭 Minor。

首次复核发现的 M-1（Q3、Q13 的 `answer_verification_status` 语义不一致）已关闭：Q3、Q13 现均为 `checked_pass`，Q7 因来源答案确需补全最终拐点坐标而保留 `corrected_verified`，Q8 因评分标记校订保留 `corrected_verified`；与各题 `checks.answer_mathematics` 及校订类型一致。

### 最终机器复核摘要

- 记录数14，唯一 `question_id` 14，校订对象总数35。
- 每题恰有一条答案文件/成员校订及一条对应成员SHA-256校订；14条哈希旧值、新值、来源和证据全部正确。
- 所有必填字段、检查枚举、校订字段、校订来源、主类型、标签、Level及六维取值通过独立结构验证，错误数0。
- Task 3 单元测试10项全部通过；正式 SQLite 与两份冻结 MMD 压缩包哈希保持基线值。

## 最终结论

最终“应试训练”审计在题目覆盖、题干、数学答案、Q3/Q7/Q13分值、Q7拐点、受控分类、难度、校订证据、来源成员哈希联动和生命周期判定上均可靠；本轮独立复核没有剩余 Critical、Important 或 Minor。14题继续保持 `audit_pending` 是因为校订尚未实际写回 `complete_questions_45.json`／正式记录，而不是审计证据不足。Joy确认前仍不得标记 `approved_for_import`。
