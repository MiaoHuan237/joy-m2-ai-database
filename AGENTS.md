# AGENTS.md

## 工作原则

1. 执行前判断歧义和影响；只有会显著改变结果、扩大范围或造成不可逆影响时才暂停询问。
2. 只完成当前任务要求，优先最小、清晰、可维护的实现。
3. 精准修改，不顺手重构、改格式或处理无关问题。
4. 多步骤任务先说明计划和验证方法；完成前必须回读实际结果并运行相应测试。
5. 使用简洁、自然的中文，先报告结果，再补充必要说明。

## 项目保护规则

- `releases/V1.18/` 是只读黄金基线。禁止编辑、重写、重新格式化、删除或替换其中任何文件。
- 禁止修改497道正式题以及题干、答案身份、难度、标签、来源、图片、校订和可抽取状态等业务数据。
- SQLite 是唯一正式事实源；CSV、Markdown和报告仅作为受验证的派生资产。
- 不拆分小问，不创建小问级正式记录。
- 修改工程结构或验证代码前后，必须运行黄金包原生验证器和根级回归测试。
- 任何未来数据变更必须另立任务、使用新版本和迁移记录，不得直接修改 V1.18。

## 标准命令

```bash
python -m unittest discover -s tests -v
python -m joy_m2 verify-baseline
python releases/V1.18/verify_task6_release.py releases/V1.18
```
